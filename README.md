# teplo_anal

Интерактивное веб-приложение для аналитики инфраструктуры теплосетей.

## Запуск

```bash
pip install -r requirements.txt
streamlit run app.py
```

По умолчанию приложение ищет CSV в директории `data/`.
Путь можно изменить в боковой панели.
